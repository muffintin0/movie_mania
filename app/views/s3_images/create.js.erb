<% if @image.new_record? %>
  alert("Failed to upload image: <%= j @image.errors.full_messages.join(', ').html_safe %>");
<% else %>
  //$("#new_movie_form").append('<input type="hidden" multiple="multiple" name="images[][url]" value="<%= j @image.image_url %>"/>');
<% end %> 